# 오답노트
- 어떤 자리에 이전과 동일한 수가 나왔음을 감지하기 위해 기존 `arr`에 남아있던 값을 사용.
- 어떤 자리 왼쪽의 값이 다르더라도 우연히 `arr`에 남아있던 값이 동일한 수이면 건너뛰어버리는 문제.
- 어떤 자리에 이전과 동일한 수가 나왔음을 감지할 때, 왼족 값들까지 전부 동일함을 보장해야함.
- 왼쪽 값들이 다 채워진 시점 + 현재 자리에 넣으려고 하는 시점 = `func`의 반복문이므로 각 반복문에서만 `pre`를 체크해주면 간단하게 구현 가능.